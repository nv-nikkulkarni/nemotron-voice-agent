{{- if and .Values.llmLightning.enabled (not .Values.llmLightning.cache.nvcf) -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "nemotron-voice-agent.fullname" . }}-llm-lightning-cache
  labels:
    {{- include "nemotron-voice-agent.labels" . | nindent 4 }}
    app.kubernetes.io/component: llm-lightning
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.llmLightning.cache.storageClass }}
  storageClassName: {{ .Values.llmLightning.cache.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.llmLightning.cache.size }}
{{- end }}
