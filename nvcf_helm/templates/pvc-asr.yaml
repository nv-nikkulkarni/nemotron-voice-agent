{{- if and .Values.asr.enabled (not .Values.asr.cache.nvcf) -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "nemotron-voice-agent.fullname" . }}-asr-cache
  labels:
    {{- include "nemotron-voice-agent.labels" . | nindent 4 }}
    app.kubernetes.io/component: asr
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.asr.cache.storageClass }}
  storageClassName: {{ .Values.asr.cache.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.asr.cache.size }}
{{- end }}
