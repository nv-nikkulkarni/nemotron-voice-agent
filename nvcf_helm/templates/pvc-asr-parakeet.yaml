{{- if and .Values.asrParakeet.enabled (not .Values.asrParakeet.cache.nvcf) -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "nemotron-voice-agent.fullname" . }}-asr-parakeet-cache
  labels:
    {{- include "nemotron-voice-agent.labels" . | nindent 4 }}
    app.kubernetes.io/component: asr-parakeet
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.asrParakeet.cache.storageClass }}
  storageClassName: {{ .Values.asrParakeet.cache.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.asrParakeet.cache.size }}
{{- end }}
