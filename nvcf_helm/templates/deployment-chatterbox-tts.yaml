{{- if .Values.chatterboxTts.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nemotron-voice-agent.fullname" . }}-chatterbox-tts
  labels:
    {{- include "nemotron-voice-agent.labels" . | nindent 4 }}
    app.kubernetes.io/component: chatterbox-tts
spec:
  replicas: 1
  progressDeadlineSeconds: 7200
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "nemotron-voice-agent.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: chatterbox-tts
  template:
    metadata:
      labels:
        {{- include "nemotron-voice-agent.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: chatterbox-tts
    spec:
      {{- include "nemotron-voice-agent.imagePullSecrets" . | nindent 6 }}
      securityContext:
        runAsUser: 0
        runAsGroup: 0
      containers:
        - name: nim-chatterbox-tts
          image: "{{ include "nemotron-voice-agent.chatterboxImage" . }}"
          imagePullPolicy: {{ .Values.chatterboxImage.pullPolicy }}
          {{- include "nemotron-voice-agent.nimStartCommand" . | nindent 10 }}
          env:
            {{- include "nemotron-voice-agent.ngcApiKeyEnv" . | nindent 12 }}
            - name: NIM_HTTP_API_PORT
              value: "9000"
            - name: NIM_GRPC_API_PORT
              value: "50051"
            {{- if .Values.chatterboxTts.nimTagsSelector }}
            - name: NIM_TAGS_SELECTOR
              value: {{ .Values.chatterboxTts.nimTagsSelector | quote }}
            - name: NVIDIA_VISIBLE_DEVICES
              value: {{ .Values.chatterboxTts.gpuDevices | quote }}
            {{- end }}
          ports:
            - name: grpc
              containerPort: 50051
              protocol: TCP
            - name: http
              containerPort: 9000
              protocol: TCP
          # TTS model init is slow — allow up to 3000 s (60 s × 50).
          {{- if .Values.nimReadyImmediate }}
          # STAGING (nimReadyImmediate): report Ready as soon as the container is running so NVCF
          # marks the function ACTIVE without waiting for the ~15-20 min model-load / TRT-engine
          # rebuild (which exceeds NVCF's pod-readiness deadline). NIMs warm ASYNC; liveness is
          # delayed 40 min so it does not kill the container mid-load.
          livenessProbe:
            httpGet:
              path: /v1/health/ready
              port: http
            initialDelaySeconds: 2400
            periodSeconds: 60
            timeoutSeconds: 15
            failureThreshold: 10
          {{- else }}
          startupProbe:
            httpGet:
              path: /v1/health/ready
              port: http
            periodSeconds: 60
            failureThreshold: 50
            timeoutSeconds: 10
          livenessProbe:
            httpGet:
              path: /v1/health/ready
              port: http
            periodSeconds: 60
            timeoutSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /v1/health/ready
              port: http
            periodSeconds: 60
            timeoutSeconds: 10
            failureThreshold: 3
          {{- end }}
          volumeMounts:
            - name: nim-cache
              mountPath: /opt/nim/.cache
            - name: shm
              mountPath: /dev/shm
          {{- with .Values.chatterboxTts.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: nim-cache
          {{- if .Values.chatterboxTts.cache.nvcf }}
          emptyDir:
            sizeLimit: {{ .Values.chatterboxTts.cache.size }}
          {{- else }}
          persistentVolumeClaim:
            claimName: {{ include "nemotron-voice-agent.fullname" . }}-chatterbox-tts-cache
          {{- end }}
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: {{ .Values.chatterboxTts.shmSize }}
      {{- with .Values.chatterboxTts.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.chatterboxTts.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.chatterboxTts.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
