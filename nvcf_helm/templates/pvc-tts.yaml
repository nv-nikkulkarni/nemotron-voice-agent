{{- if and .Values.tts.enabled (not .Values.tts.cache.nvcf) -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "nemotron-voice-agent.fullname" . }}-tts-cache
  labels:
    {{- include "nemotron-voice-agent.labels" . | nindent 4 }}
    app.kubernetes.io/component: tts
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.tts.cache.storageClass }}
  storageClassName: {{ .Values.tts.cache.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.tts.cache.size }}
{{- end }}
